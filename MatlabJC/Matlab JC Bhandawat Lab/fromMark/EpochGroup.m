classdef EpochGroup < handle
    % A simple epoch group class.
    
    properties
        epochs      % An array of epochs.
        results     % A struct of results from analysis performed on this group of epochs.
    end
    
    methods
    end
    
end

